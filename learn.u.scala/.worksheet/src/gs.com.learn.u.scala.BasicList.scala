package gs.com.learn.u.scala

object BasicList {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(92); 
  println("Welcome to the Scala worksheet");$skip(36); 
  
  val fstLst = new List[1,2,3,4];System.out.println("""fstLst  : <error> = """ + $show(fstLst ));$skip(33); 
  val sndLset = List[9,10,11,12];System.out.println("""sndLset  : <error> = """ + $show(sndLset ))}
}
